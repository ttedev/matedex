version: '3.8'

services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: matedex
      POSTGRES_PASSWORD: matedex
      POSTGRES_DB: matedex
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: